version: '3.8'

services:
  memos:
    image: neosmemo/memos:latest
    container_name: memos
    restart: unless-stopped
    ports:
      - "5230:5230"
    volumes:
      - memos_data:/var/opt/memos
    environment:
      - MEMOS_MODE=prod

volumes:
  memos_data:
    driver: local
