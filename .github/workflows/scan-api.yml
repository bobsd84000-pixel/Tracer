name: Scan API CI

on:
  push:
    paths:
      - 'api/**'
      - 'scripts/**'
      - 'package.json'
      - '.github/workflows/scan-api.yml'
  pull_request:
    paths:
      - 'api/**'
      - 'scripts/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm install
      - run: npm run build
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 scripts/scan_api.py --help || echo "OK"
